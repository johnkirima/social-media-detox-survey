kind = "web"
previewPath = "/"
title = "Social Media Detox Survey"
version = "1.0.0"
id = "artifacts/detox-survey"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22216

[services.development]
run = "pnpm --filter @workspace/detox-survey run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/detox-survey", "run", "build" ]
publicDir = "artifacts/detox-survey/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22216"
BASE_PATH = "/"
